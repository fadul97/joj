#ifndef _JOJ_SHADER_MODEL_H
#define _JOJ_SHADER_MODEL_H

namespace joj
{
    /**
     * @brief ShaderModel is an enum class that represents the shader version
     * to be used when compiling the shader.
     * 
     * Default is the default shader model of the Renderer.
     */
    enum class ShaderModel
    {
        Default
    };
}

#endif // _JOJ_SHADER_MODEL_H